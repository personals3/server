# =============================================================================
# Secrets — NEVER commit these
# =============================================================================
.env
.env.*
# Template stays in git — keep its negation on its own line; gitignore
# treats inline `#` as part of the pattern, not as a comment.
!.env.example
*.pem
*.key
*.pfx
cloudflared/*.json     # tunnel credentials
~/.aws/                # in case anyone runs from $HOME accidentally

# =============================================================================
# User data — leading slash anchors to repo root so this only matches the
# top-level bind-mount dir, NOT `api/internal/storage/` (the Go package).
# =============================================================================
/storage/
!/storage/.gitkeep

# =============================================================================
# Go
# =============================================================================
api/bin/
api/tmp/
api/server               # local-built binary
*.exe
# Keep go.sum (reproducible builds)

# =============================================================================
# Node / Next.js
# =============================================================================
**/node_modules/
**/.next/
dashboard/out/
dashboard/build/
dashboard/.swc/

# =============================================================================
# Python
# =============================================================================
**/__pycache__/
worker/.venv/
worker/*.egg-info/
*.pyc
.pytest_cache/

# =============================================================================
# Editors / OS / Misc
# =============================================================================
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
*.log
# Anchored — was matching dashboard/app/dashboard/admin/logs/ (the admin
# logs page directory) and silently excluding it from git. Same class of
# bug we already fixed for /storage/ and cli's /ps3.
/logs/

# =============================================================================
# Local-only helpers
# =============================================================================
/tmp/
*.local

# =============================================================================
# Local backup outputs (postgres dumps, storage tarballs) — never in git
# =============================================================================
/backups/

# =============================================================================
# CLI lives in its own repo (github.com/personals3/cli) — exclude from server
# =============================================================================
/cli/
docs/
plan-1.md
FUTURE_PLANS.md
.claude